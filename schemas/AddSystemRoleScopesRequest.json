{
    "id": "AddSystemRoleScopesRequest",
    "description": "Request for adding scopes to a system role.",
    "additionalProperties": false,
    "properties": {
        "role": {
            "type": "string",
            "description": "The role name to add the scope(s) to.",
            "faker": "name.jobType"
        },
        "scopes": {
            "type": "array",
            "description": "Scopes to add to the role.",
            "items": {
                "type": "string",
                "description": "The scopes",
                "faker": "lorem.word"
            }
        }
    },
    "required": [
        "scopes",
        "role"
    ]
}