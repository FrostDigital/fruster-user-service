{
    "id": "GetUsersByQueryResponse",
    "description": "Response from get users by query. Response data may vary depending on configuration.",
    "additionalProperties": false,
    "properties": {
        "totalCount": {
            "type": "number",
            "description": "The total count of results in the databse found with provided query",
            "minimum": 0
        },
        "users": {
            "$ref": "UserListResponse"
        }
    }
}