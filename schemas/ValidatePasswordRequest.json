{
    "id": "ValidatePasswordRequest",
    "description": "Request for validating password.",
    "additionalProperties": false,
    "properties": {
        "username": {
            "type": "string",
            "description": "The username of the account to validate. Determined by config.USERNAME_VALIDATION_DB_FIELD."
        },
        "password": {
            "type": "string",
            "description": "The password to validate against account with."
        }
    },
    "required": [
        "username",
        "password"
    ]
}