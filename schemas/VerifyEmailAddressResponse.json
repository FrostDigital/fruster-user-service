{
    "id": "VerifyEmailAddressResponse",
    "description": "Response from verifying email address.",
    "additionalProperties": true,
    "properties": {
        "verifiedEmail": {
            "type": "string",
            "faker": "internet.email",
            "description": "The verified email."
        }
    },
    "required": [
        "verifiedEmail"
    ]
}