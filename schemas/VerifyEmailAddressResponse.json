{
    "id": "VerifyEmailAddressResponse",
    "description": "TODO:",
    "additionalProperties": true,
    "properties": {
        "verifiedEmail": {
            "type": "string",
            "faker": "internet.email"
        }
    },
    "required": [
        "verifiedEmail"
    ]
}