{
    "id": "RoleModelListResponse",
    "description": "Response with an array of roles.",
    "anyOf": [{
            "type": "array",
            "items": {
                "$ref": "RoleModel"
            }
        },
        {
            "type": "string"
        }
    ]

}