{
    "id": "RoleModelListResponse",
    "description": "Response with an array of roles.",
    "additionalProperties": false,
    "anyOf": [
        {
            "type": "array",
            "items": {
                "$ref": "RoleModel"
            }
        },
        {
            "type": "string"
        }
    ]
}