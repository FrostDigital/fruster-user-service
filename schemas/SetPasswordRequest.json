{
    "id": "SetPasswordRequest",
    "description": "Request for setting password of a user. Used by password reset service. Note: Updating a user's password should be done w/ the update-password endpoint. ",
    "additionalProperties": false,
    "properties": {
        "id": {
            "type": "string",
            "description": "Id of the user to set password for."
        },
        "newPassword": {
            "type": "string",
            "description": "The new password to set for the user."
        }
    },
    "required": [
        "id",
        "newPassword"
    ]
}