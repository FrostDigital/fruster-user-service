{
    "id": "UpdatePasswordHttpRequest",
    "description": "Request for updating password of auth user.",
    "additionalProperties": false,
    "properties": {
        "newPassword": {
            "type": "string",
            "description": "The new password to update with."
        },
        "oldPassword": {
            "type": "string",
            "description": "The old password of the user, requires this to be validated against the account details."
        }
    },
    "required": [
        "newPassword",
        "oldPassword"
    ]
}