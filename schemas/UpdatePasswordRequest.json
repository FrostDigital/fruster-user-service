{
    "id": "UpdatePasswordRequest",
    "description": "Request for updating password of a user.",
    "additionalProperties": false,
    "properties": {
        "newPassword": {
            "type": "string",
            "description": "The new password to update with."
        },
        "oldPassword": {
            "type": "string",
            "description": "The old password of the user, requires this to be validated against the account details."
        },
        "id": {
            "type": "string",
            "description": "The id of the user to update password for."
        }
    },
    "required": [
        "newPassword",
        "oldPassword",
        "id"
    ]
}